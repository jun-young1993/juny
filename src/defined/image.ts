const BASE_PATH = '/images';

const GITHUB: string = `${BASE_PATH}/github.svg`;
const BLOG: string = `${BASE_PATH}/blog.svg`;
const CALENDAR: string = `${BASE_PATH}/calendar.svg`;
const ME: string = `${BASE_PATH}/me.png`;
const GUEST_BOOK: string = `${BASE_PATH}/guest-book.svg`;
const _IMAGE  = {
    GITHUB,
    BLOG,
    CALENDAR,
    ME,
    GUEST_BOOK
}
export default _IMAGE;
