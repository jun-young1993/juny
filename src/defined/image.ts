const BASE_PATH = '/images';

const GITHUB: string = `${BASE_PATH}/github.png`;
const BLOG: string = `${BASE_PATH}/blog.png`;
const CALENDAR: string = `${BASE_PATH}/calendar.svg`;
const _IMAGE  = {
    GITHUB,
    BLOG,
    CALENDAR
}
export default _IMAGE;
